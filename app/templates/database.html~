{% extends "layout2.html" %}
 
{% block content %}
<div class="jumbo">
  
  {% for message in form.searchterm.errors %}
  <div class="flash">{{ message }}</div>
  {% endfor %}
  
  <form action="{{ url_for('database') }}" method=post>
    
    {{ form.hidden_tag() }}
 
    {{ form.searchterm.label }} 
    {{ form.searchterm}} 
    {{ form.submit }}
    <ul class="upload"><a href="{{ url_for('upload') }}">Upload</a></ul>
  
  </form>
  </div>


<div>


{% if doc %}
  {% for d in doc %}
       <ul><a href="{{ url_for('document',id=d.id) }}">{{ d.title }}</a>{{ d.description }}{{ d.uploader_id }}{{ d.timestamp }}</ul>
  {% endfor %}
{% endif %}

</div>

<div>

{#
{% if rec|length %}
  {% for d in rec %}
       <ul><a href="{{ url_for('document',id=d.id) }}">{{ d.title }}</a>{{ d.description }}{{ d.uploader_id }}{{ d.timestamp }}</ul>
  {% endfor %}
{% else %}
       <a href="{{ url_for('document',id=d.id) }}">{{ rec.title }}</a>{{ rec.description }}{{ rec.uploader_id }}{{ rec.timestamp }}
{% endif %}
#}

<a href="{{ url_for('document',id=d.id) }}">{{ rec.title }}</a>{{ rec.description }}{{ rec.uploader_id }}{{ rec.timestamp }}
</div>




{% endblock %}
